name = "lunch-calendar"
pages_build_output_dir = "."
compatibility_date = "2024-09-23"

# KV 绑定在 Cloudflare 控制台（或 wrangler pages dev --kv LUNCH_KV）配置，
# 变量名必须为 LUNCH_KV，与 functions/api/*.js 中的 env.LUNCH_KV 对应。
